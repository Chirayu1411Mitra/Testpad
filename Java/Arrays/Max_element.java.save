class Result {
  static int maxElement(int[] arr, int N) {
    int max=Integer.MIN_VALUE;
     for(int i=0;i<N;i++){
         if(arr[i]>max){
             max=arr[i];
         }
     }
      return max;
  }
}
